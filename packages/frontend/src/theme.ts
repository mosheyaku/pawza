import { createTheme } from '@mui/material';

// Create a custom theme
export const theme = createTheme({
  palette: {
    primary: {
      main: '#ECB16B',
      light: '#ECB16B',
      dark: '#ECB16B',
    },
    secondary: {
      main: '#3F51B5',
      light: '#ECB16B',
      dark: '#ECB16B',
    },
  },
});
